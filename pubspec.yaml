name: alhora_project
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  firebase_core: ^3.4.1
  firebase_auth: ^5.2.0
  cloud_firestore: ^5.4.1
  google_fonts: ^3.0.1  # Added version for google_fonts package

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - Assets/Images/carpic.png
    - Assets/Images/bmw_logo.png
